using UnityEngine;
namespace HighElixir.UI
{
    public class ToggleableHolder : MonoBehaviour
    {
        //[SerializeField] private List<ToggleableButton> _buttons = new();
        //private Dictionary<ToggleableButton, IDisposable> _unSubscriber = new();
        //private void Awake()
        //{
        //    foreach (ToggleableButton button in _buttons)
        //        _unSubscriber[button] = button.onClick.AsObservable().Subscribe().AddTo(this);
        //}
    }
}